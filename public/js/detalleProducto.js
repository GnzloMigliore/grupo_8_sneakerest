/*window.addEventListener('load', function(){
    
    
    let ampliarFotos = document.querySelectorAll('.fotos-ampliadas');
    //console.log(ampliarFotos);
    for (let i=0; i < ampliarFotos.length; i++){
        addEventListener('click', function() {
            //console.log(ampliarFotos[i])
            if (ampliarFotos[i].classList.contains('col-5')) {
                ampliarFotos[i].classList.remove = ('col-5');
                ampliarFotos[i].classList.add = ('imagen-agrandada');
             };
        })
        
    };

    
    
    /*let subtitulo = document.getElementsByClassName('fotos-ampliadas');
    //console.log(subtitulo);
    subtitulo.addEventListener('mouseover', function(){
        subtitulo.style.color = 'white';
        subtitulo.style.backgroundColor = 'tomato';
    })*/  
