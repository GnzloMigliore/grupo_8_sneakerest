<!doctype html>
<html lang="es">
<head>
    <%- include('../partials/head.ejs') %>
    <link rel="stylesheet" href="/css/administrar.css">
</head>

<body>
    <%- include('../partials/navBar.ejs') %>
    <main class="container">
        <section class="row mx-auto mb-5 pb-5">
            <article class="col-12 p-auto mb-5">
                <div class="circulo-imagen mx-auto my-5">
                    <img src="/images/usuarios/<%= unUsuario.imagen %>" alt="<%= unUsuario.nombre + " "%><%= unUsuario.apellido %>">
                </div>
                <div class="col-6 border-bottom mx-auto mt-5 mb-4 pt-3">
                    <div class="col-12 d-flex justify-content-around align-items-center">
                        <p class="col-4 d-flex justify-content-start">Nombre y apellido: </p>
                        <p class="col-8 d-flex justify-content-start"><%= unUsuario.nombre + " "%><%= unUsuario.apellido %> </p>
                    </div>
                </div>
                <div class="col-6 border-bottom mx-auto mt-3 mb-4">
                    <div class="col-12 d-flex justify-content-around align-items-center">
                        <p class="col-4 d-flex justify-content-start">Email: </p>
                        <p class="col-8 d-flex justify-content-start"><%= unUsuario.email %> </p>
                    </div>
                </div>
                <div class="col-6 border-bottom mx-auto mt-3 mb-4">
                    <div class="col-12 d-flex justify-content-around align-items-center">
                        <p class="col-4 d-flex justify-content-start">Género: </p>
                        <p class="col-8 d-flex justify-content-start"><%= unUsuario.genero %> </p>
                    </div>
                </div>
                <div class="col-6 border-bottom mx-auto mt-3 mb-4">
                    <div class="col-12 d-flex justify-content-around align-items-center">
                        <p class="col-4 d-flex justify-content-start">Rol: </p>
                        <p class="col-8 d-flex justify-content-start"><%= unUsuario.role %> </p>
                    </div>
                </div>                 
            </article>
        </section>
    </main>
    <!-- FOOTER -->
    <%- include('../partials/footer.ejs') %>    
</body>
</html>