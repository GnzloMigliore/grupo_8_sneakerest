<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head.ejs') %>
    <link rel="stylesheet" href="/css/administrar.css">
    <link rel="stylesheet" href="/css/navbar.css">
</head>
<body>
    <%- include('../partials/navBar.ejs') %>
    <main class="container">
        <section class="row mx-auto mb-5 pb-5">
            <article class="col-12 p-auto mb-5">
                <form action="/adminUsers/edit/<%= usuarios.id %>?_method=PUT" method="POST">
                    <div class="circulo-imagen mx-auto my-5">
                        <img src="/images/usuarios/<%= usuarios.image %>" alt="<%= usuarios.first_name + " "%><%= usuarios.last_name %>">
                    </div>
                    <div class="col-6 border-bottom mx-auto mt-5 mb-4 pt-3">
                        <div class="col-12 d-flex justify-content-around align-items-center">
                            <p class="col-4 d-flex justify-content-start">Nombre y apellido: </p>
                            <p class="col-8 d-flex justify-content-start"><%= usuarios.first_name + " "%><%= usuarios.last_name %> </p>
                        </div>
                    </div>
                    <div class="col-6 border-bottom mx-auto mt-3 mb-4">
                        <div class="col-12 d-flex justify-content-around align-items-center">
                            <p class="col-4 d-flex justify-content-start">Email: </p>
                            <p class="col-8 d-flex justify-content-start"><%= usuarios.email %> </p>
                        </div>
                    </div>
                    <div class="col-6 border-bottom mx-auto mt-3 mb-4">
                        <div class="col-12 d-flex justify-content-around align-items-center">
                            <p class="col-4 d-flex justify-content-start">Género: </p>
                            <p class="col-8 d-flex justify-content-start"><%= usuarios.gender %> </p>
                        </div>
                    </div>
                    <div class="col-6 border-bottom mx-auto mt-3 mb-4">
                        <div class="form-group col-12 d-flex justify-content-around align-items-center">
                            <p class="col-4 d-flex justify-content-start mb-0">Rol: </p>
                            <input type="number" name="role" class="form-control col-8 d-flex justify-content-start" value="<%= usuarios.role %>" min="1" max="9">
                        </div>
                    </div>               
                    <div class="col-6 mx-auto mt-3 mb-4">
                        <div class="form-group col-12 d-flex flex-wrap justify-content-center">
                            <button type="submit" class="btn btn btn-dark col-12 mb-3">Actualizar</button>
                            <a href="/adminUsers" class="btn btn-white text-body border col-12">Volver</a>
                        </div>
                    </div>
   
                </form>                 
            </article>
        </section>
    </main>
    <!-- FOOTER -->
    <%- include('../partials/footer.ejs') %>    
</body>
</html>
